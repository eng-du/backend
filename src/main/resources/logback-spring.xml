<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <!-- properties 파일 로드 -->
  <property resource="logback-variables.properties" />

  <timestamp key="BY_DATE" datePattern="yyyy-MM-dd"/>

  <!-- MDC 키가 suppressSql=true 인 경우 전체 로그 레벨을 INFO 이상으로만 강제 허용 (DEBUG, TRACE 차단) -->
  <turboFilter class="ch.qos.logback.classic.turbo.DynamicThresholdFilter">
    <Key>suppressSql</Key>
    <DefaultThreshold>DEBUG</DefaultThreshold>
    <MDCValueLevelPair>
      <value>true</value>
      <level>INFO</level>
    </MDCValueLevelPair>
  </turboFilter>

  <!-- 로그 포맷 -->

  <property name="LOG_PATTERN"
    value="[%d{yyyy-MM-dd HH:mm:ss}:%-4relative] %green([%thread]) %highlight(%-5level) %replace([traceId=%X{traceId}]){'^.traceId=.$',''} %replace([userId=%X{userId}]){'^.userId=.$',''} %boldWhite([%C.%M:%yellow(%L)]) - %msg%n"/>
  <springProfile name="local">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder>
        <pattern>${LOG_PATTERN}</pattern>
      </encoder>
    </appender>

    <!-- 슬로우 쿼리 전용 로거 -->
    <logger name="org.hibernate.SQL_SLOW" level="INFO" additivity="false">
      <appender-ref ref="CONSOLE"/>
    </logger>

    <root level="DEBUG">
      <appender-ref ref="CONSOLE"/>
    </root>
  </springProfile>

  <springProfile name="dev, prod">
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
      <file>${LOG_PATH}/${LOG_FILE_NAME}.log</file>

      <encoder>
        <pattern>${LOG_PATTERN}</pattern>
      </encoder>

      <!-- 날짜 + 사이즈 롤링 -->
      <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
        <fileNamePattern>${LOG_PATH}/${LOG_FILE_NAME}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
        <maxFileSize>${LOG_MAX_FILE_SIZE}</maxFileSize>
        <maxHistory>${LOG_MAX_HISTORY}</maxHistory>
        <totalSizeCap>${LOG_TOTAL_SIZE_CAP}</totalSizeCap>
      </rollingPolicy>
    </appender>

    <!-- 슬로우 쿼리 전용 로거 -->
    <logger name="org.hibernate.SQL_SLOW" level="INFO" additivity="false">
      <appender-ref ref="FILE"/>
    </logger>

    <root level="INFO">
      <appender-ref ref="FILE"/>
    </root>
  </springProfile>
</configuration>